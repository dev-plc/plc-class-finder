# 기수 전환 체크리스트 (2기 → 3기)

## 먼저: 새 시트를 만들어야 하나?

**아니요. 지금 쓰는 시트를 그대로 3기용으로 쓰고, 2기는 사본으로 빼 두시는 게 낫습니다.**

즉 **"현재 시트 = 항상 현재 기수"** 규칙입니다.

| | 현재 시트 재사용 (권장) | 새 시트 만들기 |
|---|---|---|
| 구글 폼 응답 | **그대로** | 폼마다 응답 대상 시트를 옮겨야 함 |
| GAS | **손댈 것 없음** | `SHEET_ID` 교체 + 재배포 |
| Actions 시크릿 | 그대로 | 그대로 |
| 2기 원본 | DB + 시트 사본 | DB + 원본 시트 |
| 실수 여지 | 시트 정리할 때 | 폼·배포 연결할 때 |

**핵심은 2기 데이터가 이미 DB에 통째로 들어가 있다는 것입니다.**
인원 142명, 출석, 과제, 김밥 전부요. 시트는 더 이상 2기의 유일한 원본이 아닙니다.
그러니 시트를 얼려 둘 이유가 약하고, 대신 **폼 재연결과 GAS 재배포라는
가장 실수하기 쉬운 두 단계를 통째로 건너뛸 수 있습니다.**

> 사본을 뜨면 구글이 연결된 폼의 사본도 함께 만들 수 있습니다.
> **원본 폼은 원본 시트에 계속 연결되므로 동작에는 영향이 없습니다.**
> 생긴 폼 사본은 헷갈리지 않게 이름을 바꾸거나 지우세요.

---

## 안전장치: 시트에 기수를 적어 두세요

기수 전환에서 가장 위험한 순간은 이렇습니다.

> 시트는 이미 3기인데 매일 도는 동기화가 아직 2기로 돈다
> → 3기 명단이 2기로 들어가고, 기존 2기 인원은 "시트에서 사라졌다"고 판단돼
> 전부 `inactive` 로 내려간다. 2기 수료 기록이 화면에서 사라진다.

그래서 **출석부(DB) 상단 아무 칸에 `3기` 라고 적어 두세요.**
(A1 이든 강의명 행 왼쪽 끝이든, 위쪽 6행 × 왼쪽 12열 안이면 어디든 찾습니다)

동기화가 시트의 기수와 대상 기수를 대조해서, 다르면 **아무것도 쓰지 않고 멈춥니다.**

```
❌ 시트는 3기 인데 2기 로 동기화하려 합니다. 중단합니다.
   그대로 진행하면 3기 명단이 2기 로 들어가고,
   기존 2기 인원은 전부 inactive 로 내려갑니다.
```

이 표식만 있으면 순서를 잘못 밟아도 사고가 나지 않습니다.

---

## 0. GAS 404 부터

지금 동기화가 `HTTP 404` 로 죽고 있습니다. **코드 문제가 아닙니다.**
`GAS_API_URL` 이 가리키는 배포가 더 이상 존재하지 않는 것입니다.

1. Apps Script → **배포 → 배포 관리**
2. 활성 배포가 있는지 확인
   - 있으면: **웹 앱 URL** 복사
   - 없으면: 새로 배포 (유형 **웹 앱**, 실행 **나**, 액세스 **모든 사용자**)
3. GitHub → Settings → Secrets and variables → Actions → `GAS_API_URL` 갱신
4. 시크릿 창(로그인 안 된 창)에서 그 URL 을 열어 JSON 이 나오는지 확인

확인할 것:
- URL 이 **`/exec`** 로 끝나야 합니다. `/dev` 는 본인만 접근 가능해서 Actions 에서 404 가 납니다
- 앞뒤 공백이나 줄바꿈이 섞이지 않았는지

> **다음부터는** "새 배포" 말고 **기존 배포 → ✏️ → 버전: 새 버전 → 배포** 를 쓰세요.
> "새 배포"는 매번 새 URL 을 만듭니다. 이번 404 의 가장 흔한 원인입니다.

**이때 GAS 코드도 v24 로 함께 올려 주세요.** `scripts/gas/doGet.js` 전체를 복사해
Apps Script 에 붙여넣고 배포하면 됩니다.
v24 여야 아래 2번의 **강의명 행**과 **기수 표식**을 읽습니다.
(시트 ID 는 그대로 두세요 — 같은 시트를 계속 쓰니까요.)

---

## 1. 2기 사본 떠 두기

현재 시트 → **파일 → 사본 만들기** → 이름 `PLC 교리교육 2기 (보관)`

사본은 보조 백업입니다. 진짜 2기 원본은 DB 에 있습니다.
사본을 뜬 뒤 **원본 시트에서 계속 작업**합니다. (사본을 건드리지 마세요)

---

## 2. 현재 시트를 3기로 정리

**`출석부(DB)`**
- **상단 아무 칸에 `3기` 라고 적기** ← 엉뚱한 기수로 동기화되는 걸 막는 표식
- 명단을 3기 인원으로 교체 (2기 이월자 + 신규)
- 날짜 컬럼 헤더를 3기 일정으로 교체
- **날짜 헤더 바로 윗줄에 `강의명 행` 을 넣어 주세요** ← 3기에서 새로 필요합니다

  |  | ← A~F 인적사항 → | | | |
  |---|---|---|---|---|
  | **강의명 행** | (비움) | `교리1` | `교리2` | `교제` |
  | **헤더 행** | … `ID` … | `09/06` | `09/13` | `09/20` |
  | 인원 | [이름] | | | |

  왜 필요한가: 지금까지는 **김밥 탭에서 세션명을 유추**했습니다.
  그런데 새 기수는 김밥 탭이 비어 있어 **교제·나눔 주차를 알 수 없습니다.**
  그러면 교제 주에도 `교리N` 이 붙어 그 뒤 번호가 전부 한 칸씩 밀리고,
  수료 분모(16강)가 어긋납니다.
  출석부에 직접 적어 두면 이 문제가 영구히 사라집니다.

  적는 값: `교리1`~`교리12`, `성경적대화1`~`성경적대화4`, `교제`, `나눔`

- **출석 셀은 사람에 따라 다릅니다.**

  | 누구 | 어떻게 |
  |---|---|
  | **새로 오는 분** | **전부 비웁니다.** `O`·`X`·`◎`·`과제` 어느 것도 남기지 마세요 |
  | **지난 기수에서 이어 듣는 분** | **지난 기수 기록을 그대로 둡니다** |

  - 출석은 DB 가 원본이고 앱에서 찍습니다. 새로 오는 분 칸에 값이 남아 있으면
    결석 수가 부풀려지고, **곧바로** 과제·소감문 안내까지 나갑니다
  - **이어 듣는 분의 값은 지우면 안 됩니다.** 그게 "어디까지 들었고 무엇이
    남았는지" 의 유일한 근거입니다. 들어갈 때 이렇게 옮겨집니다:

    | 시트 값 | DB 에 들어가는 값 | 왜 |
    |---|---|---|
    | `O` | **`◎`** | 아직 하지 않은 수업에 출석은 있을 수 없습니다. 지난 기수에 들었다는 뜻이고, 언제부터 들었는지가 여기서 드러납니다 |
    | `X` | `X` 그대로 | 지난 기수에 못 들은 주차입니다. **지금 과제+소감문으로 메울 수 있어야** 합니다 |
    | `과제` | `과제` 그대로 | 지난 기수에 결석했지만 메운 주차입니다. 보충 횟수로 세야 합니다 |
    | `◎` `-` | 그대로 | 미래 주차에도 원래 정당합니다 |

  - **`◎` 를 손으로 찍을 필요는 없습니다** — 4번의 이월이 DB 에 직접 넣습니다.
    지난 기수에 과제로 인정받은 주차가 `X`·`과제` 로 남아 있으면 이월이 `◎` 로
    바꿔 줍니다 (이미 받은 인정을 3회 한도에서 또 빼지 않게)
- 날짜 컬럼 자체는 남겨 두세요 — 이게 강의 일정의 정의입니다

> 출석 셀을 못 비웠더라도 **평소 동기화는 출석을 가져오지 않으므로** 그대로 둬도 됩니다.
> `시트 출석 가져오기` 를 켤 때만 들어옵니다.
>
> **켜기 전에 `dry_run` 과 함께 한 번 돌려 보세요.** 로그에 이렇게 나옵니다:
>
> ```
> 미래 주차 표시: ◎ 로 옮길 O 3개 · 그대로 넣을 X 12개 · 과제 2개
>    예: 홍길동1234 11/29=X / …
> ```
>
> 거기 뜬 사람이 전부 '이어 듣는 분' 이면 체크를 풀고 실제로 돌리면 됩니다.
> 새로 오는 분이 섞여 있으면 시트를 아직 안 비운 것입니다.
- 날짜 표기는 `09/06` 이든 `9/6` 이든 진짜 날짜값이든 상관없습니다
  (v23 부터 `MM/DD` 로 알아서 맞춥니다)

**`과제`**
- 지우지 않아도 됩니다. 동기화가 제출 시각으로 기수를 가립니다.
  - **기수 시작 달의 1일 이후** 제출 → 이번 기수
    (3기는 첫 강의 08/09 → `2026-08-01` 부터)
  - 그 이전 → 지난 기수, 반영 안 됨
- **오프라인·사후 제출을 손으로 적을 때는 타임스탬프 칸에 기수를 쓰세요.**

  | 타임스탬프 칸 | 처리 |
  |---|---|
  | (폼이 자동으로 넣은 날짜) | 날짜로 판별 |
  | `3기` | 3기 제출로 인정 |
  | `2기` | 지난 기수, 제외 |
  | 비어 있음 | 어느 기수인지 몰라 **제외** |

  비워 두면 그 제출은 반영되지 않아 결석 보충을 못 받습니다.

**`김밥`**
- 세션명 행 / 날짜 행을 3기 일정으로
- 인원별 신청 데이터는 비움

**`새가족링크`**
- 3기 조별 텔레그램 링크로 교체

> ⚠️ **출석 열에 "앱에서만 수정" 메모를 달아 두세요.**
> 시트와 앱 양쪽에서 고치면 동기화 방향이 무너집니다.

---

## 3. DB 에 3기 만들기

GitHub → Actions → **시트 → DB 동기화** → Run workflow

**① 먼저 미리보기**

| 입력 | 값 |
|---|---|
| 기수 ID | **비워 두세요** (시트의 `3기` 표식을 따라갑니다) |
| 이 기수를 활성 기수로 지정 | ☐ |
| dry-run | ☑ |

> 기수 ID 를 비우면 **시트가 스스로 밝힌 기수**로 들어갑니다.
> 읽어온 데이터가 그 기수 것이니 거기에 쓰는 게 언제나 맞습니다.
> 굳이 적으신다면 시트 표식과 같아야 하고, 다르면 중단됩니다.

로그에서 확인할 것:
- `GAS v24` 인가
- `기수 자동 결정: 3기 (시트의 기수 표식)` 이 나오는가
  (`시트에 기수 표식이 없습니다` 경고가 뜨면 표식을 못 찾은 것입니다)
- `세션명 출처: 출석부(DB) 강의명 행 N개` 가 나오는가
  (`세션명 정보가 없습니다` 경고가 뜨면 강의명 행을 못 읽은 것입니다)
- 인원 수가 3기 명단과 맞는가
- `강의 16개` 가 나오는가 (`교리1`~`교리12`, `성경적대화1`~`4`)
- `비강의` 목록이 교제·나눔 주와 맞는가
- `커리큘럼 범위 밖 세션` 경고가 없는가

**② 맞으면 실제 반영**

| 입력 | 값 |
|---|---|
| 기수 ID | 비워 둠 |
| 이 기수를 활성 기수로 지정 | ☑ |
| dry-run | ☐ |

`activate` 를 체크해야 2기가 비활성으로 내려가고 3기가 활성이 됩니다.
**이후 매일 도는 동기화는 활성 기수를 알아서 따라가므로 더 손댈 게 없습니다.**

---

## 4. 지난 기수 이수분(◎) 이월

2기에서 넘어온 사람이 이미 들은 주차를 3기 출석부에 ◎ 로 찍어 줍니다.

> 지난 기수에 `과제` 로 메운 주차도 함께 이월됩니다 — 다만 3회 한도(`makeup_limit`)를
> 거친 것만입니다. 이 경로는 `v_makeup_detail` 을 타므로, `views.sql` 을 재실행하지
> 않았으면 스크립트가 **멈추고 알려 줍니다** (조용히 누락되지 않게).

지금까지 손으로 하시던 일입니다.

GitHub → Actions → **지난 기수 이수분(◎) 이월** → Run workflow

| 입력 | 값 |
|---|---|
| 지난 기수 | `2기` |
| 새 기수 | `3기` |
| 실제로 기록 | ☐ (먼저 미리보기) |

누구에게 몇 개가 찍힐지 명단이 나옵니다. 맞으면 **실제로 기록** 을 체크하고 다시 실행.

- 짝은 **이름 + 전화 뒷 4자리** 가 모두 같을 때만 맺습니다
- 3기에 이미 뭔가 적혀 있는 칸은 **덮지 않습니다** (빈칸만 채움)
- 교제·나눔은 대상이 아닙니다

---

## 5. 확인

- https://classfinder.plch.kr 에서 3기 인원 조회 → 조·위치가 3기 것으로 나오는가
- `admin.html` 헤더 배지가 **`3기 · 관리자`**
- 출석 관리 탭의 주차 목록이 3기 일정인가
- 콘솔에 `members-data v33`

**앱은 코드 수정도 배포도 필요 없습니다.** `cohorts.is_active` 를 보고 따라갑니다.
이미 앱을 켜 둔 사람에게는 다음 갱신 때
`3기 명단으로 갱신되었습니다. 다시 조회해 주세요.` 안내가 뜹니다.

---

## 알아 두실 것

- **2기 데이터는 지워지지 않습니다.** `cohort_id` 로 나뉘어 DB 에 그대로 남습니다.
  2기 수료 판정도 언제든 다시 조회할 수 있습니다
  (Actions → SQL 조회 → `completion_status`, 기수만 2기로).
- **3기 커리큘럼이 2기와 다르다면** `scripts/sync-sheet-to-db.mjs` 의
  `CURRICULUM` 상수와 `supabase/views.sql` 의 `completion_required_sessions()`
  (지금 16) 를 함께 고쳐야 합니다. 같다면 손댈 것 없습니다.
- 되돌리려면 Supabase SQL Editor 에서
  `update cohorts set is_active = (id = '2기');` 한 줄이면 앱이 2기로 돌아갑니다.

---

## 굳이 새 시트를 쓰신다면

폼·배포를 새로 연결해야 합니다. 위 2번 대신:

1. 새 스프레드시트를 만들고 탭 구조를 똑같이 준비
2. 각 구글 폼 → **응답 탭 → 스프레드시트 아이콘 → 기존 스프레드시트 선택** → 새 시트
   (폼 주소는 그대로라 참여자 안내는 고칠 필요 없음)
3. `scripts/gas/doGet.js` 맨 위 `SHEET_ID` 를 새 시트 ID 로 교체
   시트 ID 는 주소창 `/d/` 와 `/edit` 사이 문자열입니다
4. Apps Script → **기존 배포 → ✏️ → 버전: 새 버전 → 배포** (URL 유지)
