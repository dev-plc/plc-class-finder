# 기수 전환 체크리스트 (2기 → 3기)

## 먼저: 새 시트를 만들어야 하나?

**네. 2기 시트를 사본으로 떠서 3기 시트를 새로 만드시는 걸 권합니다.**

출석이 DB로 넘어간 지금, 시트가 담을 것은 **명단·편성·일정** 과
**폼 응답(과제·김밥)** 뿐입니다. 예전보다 훨씬 가벼워졌습니다.

| | 새 스프레드시트 (권장) | 같은 시트에 3기 탭 추가 |
|---|---|---|
| GAS 수정 | `SHEET_ID` 한 줄 | 탭 이름 분기 로직 추가 |
| 2기 이력 | 그대로 얼어붙음 | 계속 같이 열림 (실수로 수정 위험) |
| 김밥·과제 탭 | 기수마다 깨끗 | 두 기수가 한 탭에 섞임 |
| 4기·5기 가면 | 그대로 반복 | 탭이 계속 쌓여 무너짐 |
| 구글 폼 | **응답 대상 시트를 옮겨야 함** | 그대로 |

새 시트의 유일한 단점이 폼 재연결인데, 클릭 몇 번이고 **폼 주소는 안 바뀌므로
참여자에게 다시 안내할 필요가 없습니다.**

---

## 0. 먼저 GAS 404 부터

지금 동기화가 `HTTP 404` 로 죽고 있습니다. 배포가 사라졌거나 URL 이 바뀐 것입니다.

1. Apps Script → **배포 → 배포 관리**
2. 활성 배포의 **웹 앱 URL** 복사
3. GitHub → Settings → Secrets and variables → Actions → `GAS_API_URL` 갱신
4. 시크릿 창에서 그 URL 을 열어 JSON 이 나오는지 확인

> **다음부터는** "새 배포" 말고 **기존 배포 → ✏️ → 버전: 새 버전 → 배포** 를 쓰세요.
> 그러면 URL 이 유지돼 시크릿을 다시 만질 일이 없습니다.
> ("새 배포"는 매번 새 URL 을 만듭니다. 이번 404 의 가장 흔한 원인입니다.)

---

## 1. 3기 스프레드시트 만들기

2기 시트 → **파일 → 사본 만들기** → 이름 `PLC 교리교육 3기`

각 탭을 이렇게 정리합니다.

**`출석부(DB)`**
- 명단을 3기 인원으로 교체 (2기 이월자 + 신규)
- 날짜 컬럼(`MM/DD`) 헤더를 3기 일정으로 교체
- **출석 셀은 전부 비웁니다.** 출석은 이제 DB 가 원본이고 앱에서 찍습니다
- 날짜 컬럼은 남겨 두세요 — 이게 강의 일정의 정의입니다

**`김밥`**
- 세션명 행 / 날짜 행을 3기 일정으로
- 인원별 신청 데이터는 비움

**`과제`**
- 헤더만 남기고 응답 행 삭제

**`새가족링크`**
- 3기 조별 텔레그램 링크로 교체

> ⚠️ **출석 열에 "앱에서만 수정" 메모를 달아 두세요.**
> 시트와 앱 양쪽에서 고치면 동기화 방향이 무너집니다.

---

## 2. 구글 폼 응답을 3기 시트로

과제 폼(그리고 김밥 폼을 만드셨다면 그것도):

**폼 편집 → 응답 탭 → 스프레드시트 아이콘 → 기존 스프레드시트 선택 → 3기 시트**

2기 응답은 2기 시트에 그대로 남고, 이후 응답만 3기 시트로 들어갑니다.
폼 주소는 그대로이므로 안내문을 고칠 필요가 없습니다.

---

## 3. GAS 를 3기 시트로

`scripts/gas/doGet.js` 맨 위 (v22 부터 상수로 빼 뒀습니다):

```js
var SHEET_ID = "여기에_3기_시트_ID";
```

시트 ID 는 주소창의 `/d/` 와 `/edit` 사이 문자열입니다.

```
https://docs.google.com/spreadsheets/d/【이 부분】/edit
```

바꾼 뒤 **기존 배포 → ✏️ → 버전: 새 버전 → 배포** (URL 유지).

---

## 4. DB 에 3기 만들기

GitHub → Actions → **시트 → DB 동기화** → Run workflow

**① 먼저 미리보기**

| 입력 | 값 |
|---|---|
| 기수 ID | `3기` |
| 이 기수를 활성 기수로 지정 | ☐ |
| dry-run | ☑ |

로그에서 확인할 것:
- 인원 수가 3기 명단과 맞는가
- `강의 16개` 가 나오는가 (`교리1`~`교리12`, `성경적대화1`~`4`)
- `커리큘럼 범위 밖 세션` 경고가 없는가

**② 맞으면 실제 반영**

| 입력 | 값 |
|---|---|
| 기수 ID | `3기` |
| 이 기수를 활성 기수로 지정 | ☑ |
| dry-run | ☐ |

`activate` 를 체크해야 2기가 비활성으로 내려가고 3기가 활성이 됩니다.
**이후 매일 도는 동기화는 활성 기수를 알아서 따라가므로 더 손댈 게 없습니다.**

---

## 5. 지난 기수 이수분(◎) 이월

2기에서 넘어온 사람이 이미 들은 주차를 3기 출석부에 ◎ 로 찍어 줍니다.
지금까지 손으로 하시던 일입니다.

GitHub → Actions → **지난 기수 이수분(◎) 이월** → Run workflow

| 입력 | 값 |
|---|---|
| 지난 기수 | `2기` |
| 새 기수 | `3기` |
| 실제로 기록 | ☐ (먼저 미리보기) |

누구에게 몇 개가 찍힐지 명단이 나옵니다. 맞으면 **실제로 기록** 을 체크하고 다시 실행.

- 짝은 **이름 + 전화 뒷 4자리** 가 모두 같을 때만 맺습니다
- 3기에 이미 뭔가 적혀 있는 칸은 **덮지 않습니다** (빈칸만 채움)
- 교제·나눔은 대상이 아닙니다

---

## 6. 확인

- https://classfinder.plch.kr 에서 3기 인원 조회 → 조·위치가 3기 것으로 나오는가
- `admin.html` 헤더 배지가 **`3기 · 관리자`**
- 출석 관리 탭의 주차 목록이 3기 일정인가
- 콘솔에 `members-data v33`

**앱은 코드 수정도 배포도 필요 없습니다.** `cohorts.is_active` 를 보고 따라갑니다.
이미 앱을 켜 둔 사람에게는 다음 갱신 때
`3기 명단으로 갱신되었습니다. 다시 조회해 주세요.` 안내가 뜹니다.

---

## 알아 두실 것

- **2기 데이터는 지워지지 않습니다.** `cohort_id` 로 나뉘어 그대로 남습니다.
  2기 수료 판정도 언제든 다시 조회할 수 있습니다.
- **2기 시트는 이제 건드리지 마세요.** 동기화 대상이 아니게 됩니다.
- **3기 커리큘럼이 2기와 다르다면** `scripts/sync-sheet-to-db.mjs` 의
  `CURRICULUM` 상수와 `supabase/views.sql` 의 `completion_required_sessions()`
  (지금 16) 를 함께 고쳐야 합니다. 같다면 손댈 것 없습니다.
- 되돌리려면 Supabase SQL Editor 에서
  `update cohorts set is_active = (id = '2기');` 한 줄이면 앱이 2기로 돌아갑니다.
