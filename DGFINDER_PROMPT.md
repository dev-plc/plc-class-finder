# DGfinder 인계 — 출결까지 포함한 전체 구조

DGfinder Code 대화에 **아래 `---` 사이를 통째로** 붙여넣으세요.

관련 문서 (필요할 때 함께 전달):
- `DGFINDER_HANDOFF.md` — Supabase 조회 전환 (스키마·RLS·PostgREST)
- `DGFINDER_AUTOUPDATE_PROMPT.md` — 접속하면 최신 코드가 뜨게 (SW·버전)

---

# 작업: plc-class-finder 의 구조를 DGfinder 에 옮기기

## 배경

자매 프로젝트 `plc-class-finder`(교리교육 조배치·출결 조회 웹앱)를 몇 달간 굴리면서
구조를 두 번 크게 갈아엎었습니다. 지금 형태가 세 번째이고, 그 사이 실제로 데이터가
틀어지는 사고를 여러 번 냈습니다. DGfinder 는 같은 길을 다시 걷지 않아도 됩니다.

아래는 **결론과 그 결론에 이르게 한 사고**입니다. 이유를 모르면 반드시 다시 밟습니다.

## 최종 구조

```
                    ┌─ 원본 ─────────────────┐
                    │  Google 스프레드시트    │   ← 편성·명단·출결 전부 여기
                    └────────────────────────┘
                       ↑                  │
        ① 앱에서 출석 체크              │ ② 하루 1회 동기화 (명단·편성·과제)
           → GAS doPost                  │    10분마다 트리거 (출결)
              → 시트에 쓴다              ↓
              → DB 에도 민다      ┌─ 사본 ──────────┐
                                  │   Supabase      │  ← 조회만. 빠르다.
                                  └─────────────────┘
                                          ↓
                                     앱이 읽는다
```

**원본은 시트 하나. DB 는 버려도 되는 사본.** 앱은 DB 를 직접 쓰지 않습니다.

## 왜 이 구조인가 — 두 곳에서 쓰면 반드시 어긋난다

처음에는 DB 를 원본으로 두고 앱이 직접 썼습니다. 빠르고 깔끔해 보였습니다.
그런데 관리자가 시트에서도 손으로 고쳤습니다. 두 곳에서 쓰는 순간
**어느 쪽이 최신인지 판단할 근거가 사라집니다.** 나중에 도는 쪽이 이길 뿐입니다.

실제로 난 사고: 지난 기수 이수자에게 붙은 `◎`(출석 인정)를 앱이 모르는 상태에서
튜터가 `빈칸 → 결석` 을 눌렀고, 이수자 8명이 결석으로 저장됐습니다.
수료 판정이 틀어지고 과제 제출 안내까지 잘못 나갔습니다.

**저장소를 하나로 두면 이 문제가 성립하지 않습니다.** 앱은 시트를 쓰는
또 하나의 창구가 되고, DB 는 시트를 비추기만 합니다. 어긋나도
다음 밀어넣기가 맞춰 주므로 **스스로 낫습니다.**

읽기만 DB 에서 바로 합니다. 그게 빠른 조회의 값어치이고, 사본이라 틀려도 복구가
"시트 고치고 다시 밀기" 로 끝납니다.

## 먼저 확인해 주세요

작업 전에 현재 상태를 알려 주세요.

1. DGfinder 에 출결 개념이 있는지, 있다면 지금 어디에 기록하는지
2. GAS 웹앱(`doGet`/`doPost`)이 있는지, 배포돼 있는지
3. 시트 탭 구성과 헤더 행 (날짜 컬럼 형식이 `9/6` 인지 `2026. 9. 6` 인지)
4. 사람을 무엇으로 식별하는지 (plc 는 `이름 + 전화 뒷 4자리`)

## 만들 것

### 1. 읽기 — Supabase

`DGFINDER_HANDOFF.md` 를 따르세요. 조회 전용 최소 스키마, RLS, anon 키.

### 2. 쓰기 — 앱 → GAS → 시트 → DB

앱은 **바뀐 사람만** 모아 GAS `doPost` 로 보냅니다.

```js
// 앱 쪽 (데이터 계층 안에서만)
const result = await postToGas({
  session: '2026-08-09',                    // YYYY-MM-DD
  batch: [{ name, phone, status }, ...],    // 바뀐 사람만
});
```

```js
// GAS 쪽 순서가 중요하다
// 1) 시트에 쓴다      ← 원본. 여기까지 됐으면 데이터는 안전하다.
// 2) DB 에 민다       ← 사본. 실패해도 잃는 것이 없다.
// 2 가 실패해도 저장 성공으로 응답한다. 트리거가 다음 차례에 맞춘다.
```

### 3. 자가 치유 — 10분 트리거

시트에 사람이 직접 친 값과, 위 2) 가 실패한 경우를 메웁니다.
시트와 DB 를 비교해 **다른 칸만** 밀어넣습니다.
`ScriptApp.newTrigger('pushToDb').timeBased().everyMinutes(10).create()`

---

# 반드시 지킬 것 — 전부 실제로 밟은 함정입니다

## GAS

**1. 컬럼을 통째로 읽고 쓰면 동시 저장에 기록이 사라진다.**
두 사람이 같은 시각에 저장하면 나중 사람이 먼저 사람의 변경을 덮어씁니다.
오류도 안 나서 사라진 줄도 모릅니다. `LockService.getScriptLock()` 필수입니다.

**2. `var` 이름이 겹치면 나중에 읽힌 쪽이 조용히 이긴다.**
GAS 는 프로젝트 안 `.gs` 파일을 한 덩어리로 이어 붙입니다.
다른 파일에 같은 이름의 상수가 있으면 그쪽 값으로 요청이 나갑니다.
`Invalid API key` 가 떴는데 파일의 키는 멀쩡해서 원인을 못 찾았습니다.
**프로젝트에 얹는 코드의 전역 이름에는 접두어를 붙이세요** (`DG_SUPABASE_URL`).

**3. `onOpen` 은 한 프로젝트에 하나만 산다.**
둘이면 하나가 조용히 집니다. 기존 메뉴가 통째로 사라져도 오류가 안 납니다.
**얹는 코드에서는 `onOpen` 을 정의하지 말고** 메뉴 다는 함수만 두세요.
기존 `onOpen` 안에서 한 줄 부르게 합니다.

**4. 웹앱은 `doPost` 를 고쳐도 재배포해야 반영된다.**
저장만으로는 옛 코드가 계속 돕니다. 그리고 **"새 배포" 를 하면 URL 이 바뀝니다** —
반드시 `배포 관리 → 연필 → 버전: 새 버전` 으로 기존 배포의 버전만 올리세요.
URL 이 바뀌어 404 로 며칠 헤맸습니다.

**5. 브라우저에서 GAS 를 부를 때 `application/json` 은 CORS 로 막힌다.**
preflight 때문입니다. `Content-Type: text/plain;charset=utf-8` 로 보내고
서버에서 `JSON.parse(e.postData.contents)` 하세요.

**6. `alert()` 는 시트 창에 뜬다.**
편집기에서 실행하면 "실행 중" 만 보이고 멈춘 것처럼 됩니다.
단계마다 `Logger.log` 를 남기고, 확인 창 내용도 띄우기 전에 먼저 로그에 적으세요.

**7. 배포 URL 은 인증이 없다.**
URL 을 아는 사람은 누구나 호출합니다. `doPost` 안에서 **값을 검증**하세요
(허용 상태만 통과). 그리고 응답에 민감한 컬럼을 담지 마세요 —
`headers.forEach` 로 전 컬럼을 담는 코드는 시트에 개인정보 컬럼이 생기는 순간
그걸 그대로 공개합니다.

## DB (Supabase)

**8. PostgREST 는 한 번에 1000행만 준다. 조용히 잘린다.**
인원 × 주차라 금방 넘습니다(83명 × 19주차 = 1577행).
화면은 "출석 0" 인데 DB 뷰는 10 이라고 하는 앞뒤 안 맞는 상태가 됩니다.
**`limit`/`offset` 으로 나눠 받고, `order` 를 반드시 넣으세요** —
정렬이 없으면 페이지마다 순서가 흔들려 빠지거나 겹칩니다.

**9. 쓰기는 `security definer` RPC 로만 열어라.**
anon 키는 공개됩니다. 테이블 직접 쓰기를 주지 말고, 상태값만 바꾸는
함수 하나만 `grant execute` 하세요.

**10. 판정 규칙은 SQL 뷰에 한 곳으로 모아라.**
수료·과제 대상 같은 규칙을 앱과 스크립트가 각자 계산하면 반드시 갈라집니다.

## 앱

**11. 캐시로 먼저 그리면, 갱신이 끝났을 때 화면을 다시 그려야 한다.**
`ensureLoaded()` 가 캐시로 즉시 렌더하고 뒤에서 새로 받아오는 구조라면,
`refresh` 이벤트를 받아 **모든 화면을 다시 그리세요.**
이걸 빠뜨려서 위의 `◎ → X` 사고가 났습니다. 화면에는 빈칸으로 보였고
튜터는 그게 옛 데이터인 줄 몰랐습니다.

**12. 값을 스냅숏으로 떠 놓는 화면이 특히 위험하다.**
목록은 그릴 때마다 최신을 읽지만, 체크박스 화면은 로드 시점 값을 붙들고 있습니다.
같은 데이터인데 한쪽만 옛것으로 보입니다.
편집 중이면 덮지 말고 **"새 데이터가 도착했습니다" 라고 알리세요.**

**13. 저장할 때 전원을 보내지 마라. 바뀐 사람만 보내라.**
전원을 `present ? 'O' : 'X'` 로 보내면 그 외의 상태(`◎` 이수 인정, `−` 집계 제외)가
한 번에 지워집니다. 손대지 않은 사람은 전송에서 빼세요.

**14. 일괄 버튼은 무엇이 몇 개 바뀌는지 보여주고 물어라.**
`빈칸 → 결석` 은 "모르는 것" 을 "결석" 으로 바꾸는 동작입니다.
되돌리기 어려운 변경(예: 이수 인정 → 결석)에는 한 번 더 확인을 받으세요.

**15. 백엔드를 갈아탈 때 사라지는 필드를 조심하라.**
GAS 응답에 있던 `m.attendance` · `m.lunch` 를 DB 이관 후에도 계속 읽고 있었습니다.
`undefined` 라 오류 없이 전원이 미체크·결석으로 보였습니다. **두 번 겪었습니다.**
이관 후 `git grep` 으로 옛 필드 이름을 훑으세요.

**16. 아직 하지 않은 주차는 찍을 수 없게 하라.**
미래 주차에 O/X 가 들어가면 결석 수가 부풀려져 수료 판정과 안내가 틀어집니다.
주차 선택 목록에서 아예 빼세요.

## 운영

**17. 기수(과정)가 바뀔 때 시트에 기수 표식을 두고 대조하라.**
시트는 새 기수로 갈아엎었는데 동기화가 옛 기수로 돌아 명단을 엉뚱한 곳에
밀어넣은 적이 있습니다. 시트 상단에 `3기` 를 적어 두고, 동기화가 활성 기수와
다르면 **아무것도 쓰지 않고 멈추게** 하세요.

**18. 지난 기수 이수분 이월은 명단이 바뀔 때마다 다시 돌려라.**
빈칸만 채우는 스크립트라 몇 번을 돌려도 안전합니다.
이월 이후에 등록한 사람은 이수 이력이 반영되지 않아 사고의 씨앗이 됩니다.

## 검증

작업을 마치면 아래를 실제로 해 보고 결과를 알려 주세요.

1. 앱에서 한 명만 바꿔 저장 → **시트에 즉시** 반영되는가
2. 시트에 직접 한 칸 고치고 트리거를 돌림 → **앱에 반영**되는가
3. 두 브라우저에서 동시에 저장 → **양쪽 변경이 모두 남는가** (잠금 확인)
4. 허용되지 않는 값을 `doPost` 로 보냄 → **거부되는가**
5. 이수 인정(`◎`) 인 사람이 있는 조에서 `빈칸 → 결석` → **확인을 묻고, `◎` 는 그대로인가**
6. 캐시가 있는 상태로 앱 열기 → 배경 갱신 뒤 **모든 화면이 새 값으로 바뀌는가**

---
