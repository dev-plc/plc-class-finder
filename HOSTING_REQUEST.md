# 호스팅 업체 요청문

아래 `---` 사이를 그대로 복사해 보내세요.

---

안녕하세요. **classfinder.plch.kr** 관련해 CDN 캐시 설정 조정을 요청드립니다.

## 증상

사이트를 새로 배포해도 **HTML 문서만 몇 주 전 버전이 계속 제공됩니다.**
같은 페이지 안에서 JS·CSS 파일은 최신인데 HTML 문서만 옛것입니다.

이용자들이 강력 새로고침, 시크릿 모드, 다른 브라우저, 다른 기기로 접속해도
동일하게 옛 화면이 나옵니다. 브라우저 캐시가 아니라 **엣지(CDN) 캐시**로 판단됩니다.

## 확인한 근거

1. `https://classfinder.plch.kr/admin.html`
   → 몇 주 전 HTML 이 반환됩니다.

2. `https://classfinder.plch.kr/admin.html?x=99887766`
   → **즉시 최신 HTML 이 반환됩니다.**
   (캐시에 없는 주소라 원본 서버까지 도달한 것으로 보입니다)

3. 같은 응답 안에서 `admin.js` 는 최신 내용인데 HTML 만 옛 버전입니다.
   → 원본 서버(GitHub Pages) 배포는 정상이며, HTML 만 캐시에 묶여 있습니다.

4. 시점에 따라 서로 다른 옛 버전이 반환됩니다.
   → 엣지 노드별로 캐시된 시점이 달라, 이용자마다 다른 화면을 보고 있습니다.

## 요청 사항

**1) 캐시 퍼지**
`classfinder.plch.kr` 전체 캐시를 한 번 삭제해 주세요.

**2) HTML 은 엣지 캐시에서 제외**
Cloudflare 기준으로 아래 규칙을 추가해 주시면 됩니다.

- **Cache Rules** 추가
- 조건: `URI Path` **ends with** `.html` **또는** `URI Path` **equals** `/`
- 동작: **Bypass cache**

또는 기존에 **Cache Everything** 성격의 Page Rule 이 설정돼 있다면,
그 규칙에서 HTML 을 제외하거나 **Edge TTL 을 "Respect origin headers"** 로
변경해 주셔도 됩니다. (원본 서버가 `Cache-Control: max-age=600` 을 보냅니다)

**3) 현재 설정 확인 회신**
`classfinder.plch.kr` 에 적용된 Page Rules / Cache Rules 와
Edge Cache TTL 설정값을 알려주시면 감사하겠습니다.

## 유지해 주실 것

**CSS·JS·이미지 캐싱은 그대로 두셔도 됩니다.** 해당 파일들은 주소에
`?v=48` 형태의 버전이 붙어 있어, 배포할 때마다 주소가 바뀌므로
캐시가 오래 남아도 문제가 없습니다. 오히려 성능에 도움이 됩니다.

캐시에서 빼야 하는 것은 **HTML 문서뿐**입니다. HTML 은 페이지당 3~4KB 로 작고,
이 파일 안에 위 버전 정보가 들어 있어 나머지 캐싱을 제어하는 역할을 합니다.
HTML 이 옛것이면 그 안의 버전 정보도 옛것이라, 전체가 옛 상태로 고정됩니다.

감사합니다.

---

## 회신을 받으면

- **Cache Everything 규칙이 있었다면** → 그게 원인입니다. HTML 제외로 해결됩니다.
- **Edge TTL 이 길게 설정돼 있었다면** → 그것도 원인입니다. Respect origin 으로 충분합니다.
- **"설정한 적 없다" 는 답이 오면** → Always Online 기능이나 기본 캐시 정책일 수 있습니다.
  그때는 위 2)번 Cache Rules 추가만으로도 해결됩니다.

업체 조치가 늦어져도 앱은 동작합니다. 서비스 워커가 HTML 을 받을 때
`?_sw=` 를 붙여 엣지를 우회하고, 관리자 페이지 링크에도 `?v=` 가 붙어 있습니다.
다만 그건 우회일 뿐이라, 원인은 위 설정에서 정리하는 것이 맞습니다.
