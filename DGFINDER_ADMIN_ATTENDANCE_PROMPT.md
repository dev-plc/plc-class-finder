# DGfinder 인계 — 관리자 출석 관리 화면

DGfinder Code 대화에 **아래 `---` 사이를 통째로** 붙여넣으세요.
실제 구현은 `plc-class-finder` 의 `admin.js:425~790` · `admin.html:66~98` 입니다.

먼저 전달해야 할 것: **`DGFINDER_PROMPT.md`** (출결 원본·쓰기 경로).
그게 정해지지 않으면 이 화면은 만들 수 없습니다.

---

# 작업: 관리자 출석 관리 화면

## 무엇을 만드나

관리자가 **주차와 조를 골라 조원 출결을 한 화면에서 찍고 한 번에 저장**합니다.

```
주차 [08/09 · 교리1        ▾]   조 [전체 (93명)      ▾]
08/09 교리1 · 수료 카운트에 포함

[전체 출석] [빈칸 → 결석] [전체 지우기] [되돌리기]
                    출석 75  대체 4  결석 13  제외 1  미기록 0

새A ─────────────────────────────────────
  박민수 5326  튜터          (O) ( ◎ ) ( X ) ( − )
  김승주 1917                ( O ) ( ◎ ) (X) ( − )
  최민서 8596                ( O ) (◎) ( X ) ( − )
새B ─────────────────────────────────────
  ...
──────────────────────────────────────────
2명 변경됨                        [ 2명 저장 ]
```

조를 `전체` 로 두면 전원이 조별로 묶여 한 화면에 나옵니다.
조를 옮겨 다니지 않고 한 번에 찍을 수 있어야 합니다 — 이게 이 화면의 존재 이유입니다.

## 전제 — 쓰기 경로

**출결의 원본은 시트입니다. 이 화면은 DB 를 직접 쓰지 않습니다.**
바뀐 사람만 모아 GAS 로 보내고, GAS 가 시트에 쓴 뒤 DB 에 밀어넣습니다.
자세한 것은 `DGFINDER_PROMPT.md` 를 보세요.

## 상태 모델 — 여기가 전부입니다

화면은 **두 개의 Map** 으로 돌아갑니다.

```js
let attBaseline = new Map();   // uuid → 서버에 저장돼 있는 값
let attDraft    = new Map();   // uuid → 화면에서 고른 값
```

**저장할 때 둘을 비교해 다른 것만 보냅니다.**

```js
function attChanges() {
    const out = [];
    for (const [uuid, status] of attDraft) {
        if (attBaseline.get(uuid) !== status) out.push({ memberUuid: uuid, status });
    }
    return out;
}
```

**전원을 보내면 안 됩니다.** `present ? 'O' : 'X'` 로 전원을 보내는 구조였다가
`◎`(지난 기수 이수)와 `−`(집계 제외)가 한 번에 지워지는 사고가 났습니다.
손대지 않은 사람은 전송에서 빠져야 합니다.

## 관리자가 찍는 값은 O 와 X 둘뿐이다

**이게 이 화면의 가장 중요한 규칙입니다.**

출결 값은 넷(`O ◎ X −`)이지만, 사람이 정하는 것은 둘뿐입니다.

- `◎` (지난 기수 이수) — **이월 스크립트**가 지난 기수 기록에서 뽑는다
- `−` (집계 제외) — **커리큘럼**상 수업이 없는 주차다

둘 다 판단의 근거가 사람 머릿속이 아니라 데이터에 있습니다. 손으로 찍게 두면
근거 없는 이수 인정이 생기고, 반대로 **이미 붙은 `◎` 를 실수로 지우는 사고**가 납니다.
실제로 그렇게 이수자 8명이 결석으로 저장됐습니다.

```js
// 찍을 수 있는 값
const ATT_STATES = [
    { value: 'O', label: 'O', title: '출석' },
    { value: 'X', label: 'X', title: '결석' },
];

// 보기 전용. 이 값이 든 줄은 아예 손댈 수 없게 한다.
const ATT_LOCKED = {
    '◎': { label: '◎', title: '지난 기수에 이수 — 시트에서만 고칩니다' },
    '-': { label: '−', title: '집계 제외 — 시트에서만 고칩니다' },
};
```

**`◎`·`−` 인 줄은 버튼 자리에 배지만 놓으세요.** 비활성 버튼이 아니라
아예 다른 것으로 보여야 "누를 수 있는데 막힌 것" 이 아니라 "누를 것이 아닌 것" 으로 읽힙니다.

그리고 **일괄 처리에서도 빼야 합니다.** `전체 출석`·`빈칸 → 결석`·`전체 지우기`가
`◎` 를 건드리면 버튼 하나로 조 전체의 이수 기록이 날아갑니다.

```js
function editableUuids() {
    return [...attDraft.keys()].filter(u => !isLocked(attBaseline.get(u)));
}
```

고쳐야 할 일이 생기면 **시트에서 고칩니다.** 출결의 원본이 시트이므로 그게 맞는 경로입니다.

**같은 버튼을 다시 누르면 미기록(빈칸)으로 돌아가야 합니다.**
그래야 잘못 찍은 것을 되돌릴 수 있습니다.

```js
attList.addEventListener('click', (e) => {
    const btn = e.target.closest('.att-state');
    if (!btn || attSaving) return;
    const uuid = btn.dataset.uuid;
    const next = attDraft.get(uuid) === btn.dataset.status ? '' : btn.dataset.status;
    attDraft.set(uuid, next);
    renderAttList();
});
```

**눈에 보이는 표시 두 가지를 넣으세요.**
- 미기록인 줄 → 주황 띠 (아직 안 찍은 사람이 몇 명인지 바로 보인다)
- 저장 안 한 변경 → 파란 띠 (무엇을 건드렸는지 보인다)

---

# ⚠️ 함정 — 이 화면에서 실제로 데이터가 망가졌습니다

## 1. 배경 갱신을 화면에 반영하지 않으면 사고가 난다 (가장 중요)

캐시로 먼저 그리고 뒤에서 새 데이터를 받아오는 구조라면, **받아온 뒤 이 화면을
반드시 다시 그려야 합니다.**

이 화면만 값을 `attBaseline`/`attDraft` 에 **스냅숏으로 떠 놓기** 때문입니다.
목록 화면은 그릴 때마다 최신을 읽지만 여기는 로드 시점 값을 붙들고 있습니다.

**실제로 난 사고**: DB 에는 `◎` 가 있었는데 화면에는 옛 캐시라 빈칸으로 보였고,
튜터가 `빈칸 → 결석` 을 누르는 바람에 지난 기수 이수자 8명이 결석으로 저장됐습니다.
수료 판정이 틀어지고 과제 제출 안내까지 잘못 나갔습니다.

```js
subscribe((event) => {
    if (event.type !== 'refresh') return;
    if (attChanges().length === 0) {
        initAttendanceTab();          // 편집 중이 아니면 새 값으로
    } else {
        attStale = true;              // 편집 중이면 덮지 않는다
        updateAttSummary();           // 대신 "새 데이터가 도착했습니다" 를 띄운다
    }
});
```

편집 중에 덮으면 입력하던 것이 날아갑니다. 그렇다고 조용히 두면 옛 값 기준으로
저장됩니다. **덮지 말고 알리세요.**

## 2. 일괄 버튼은 무엇이 몇 개 바뀌는지 보여주고 물어라

`빈칸 → 결석` 은 **"모르는 것" 을 "결석" 으로 바꾸는 동작**입니다.
`◎` 를 잠갔어도, 아직 `◎` 가 붙지 않은 사람은 빈칸으로 보입니다
(이월 스크립트를 돌리기 전이거나, 그 뒤에 등록한 사람).

```js
const blanks = [...attDraft].filter(([, v]) => v === '').map(([u]) => u);
if (!confirm(
    `미기록 ${blanks.length}명을 결석으로 처리합니다.\n\n${attNamesOf(blanks)}\n\n` +
    `지난 기수에 이수해 안 나와도 되는 분이 섞여 있으면\n` +
    `취소하고 그분들을 ◎ 로 먼저 표시하세요.`)) return;
```

`전체 지우기` 도 마찬가지입니다 — 이수 기록까지 함께 지웁니다.

## 3. 저장 직전에도 한 번 더 걸러라 (이중 방어)

위에서 `◎`·`−` 를 잠갔다면 `◎ → X` 는 화면에서 일어날 수 없습니다.
그래도 저장 직전 검사를 남겨 두세요. 나중에 UI 를 손볼 때 잠금이 풀려도
데이터까지 가지 않게 막아 줍니다. 몇 줄이고 비용이 없습니다.

```js
const demoted = changes.filter(c =>
    attBaseline.get(c.memberUuid) === '◎' && c.status === 'X');
if (demoted.length && !confirm(`지난 기수 이수(◎) ${demoted.length}명을 결석으로 바꿉니다. …`)) return;
```

## 4. 저장한 뒤에는 `되돌리기` 가 못 되돌린다

저장 성공 시 `attBaseline` 을 새 값으로 갱신하므로, `되돌리기` 는
"저장 전" 이 아니라 **"마지막 저장 상태"** 로 돌립니다. 이걸 모르면
잘못 저장한 뒤 되돌리기를 눌러 놓고 고쳐졌다고 착각합니다.

버튼 이름을 `되돌리기` 대신 `마지막 저장 상태로` 로 두거나,
저장 후에는 비활성화하는 편이 낫습니다.

## 5. 미기록을 결석으로 세지 마라

요약 숫자에서 **아직 안 찍은 사람은 결석이 아닙니다.**
수업 전에 화면을 열면 결석이 인원수만큼 나오는 황당한 상태가 됩니다.
`X` 로 기록된 것만 결석으로 세고, 나머지는 `미기록` 으로 따로 보여주세요.

## 6. 고른 주차·조를 기억하라

매번 처음부터 고르게 하면 안 됩니다. `localStorage` 에 저장하고,
다음에 열 때 그 값이 아직 유효한지 확인한 뒤 복원하세요.

```js
attSessionDate = (prefs.session && known.has(prefs.session))
    ? prefs.session
    : (getCurrentSessionDate() || sessions[0]?.session_date || null);
```

기본값은 **가장 최근 지난 강의**입니다. 방금 끝난 수업을 바로 찍는 게 주 용도입니다.

## 7. 주차 목록에 안 한 강의를 넣을지 정하라

관리자 화면은 **전 주차를 넣습니다** (지난 주차 정정이 주 업무).
조회 화면(튜터용)은 **지난 주차만** 넣습니다 (미리 찍히면 결석 수가 부풀려진다).
두 화면의 정책이 다르다는 것을 알고 만드세요.

## 8. 저장하지 않고 이탈하는 것을 막아라

주차·조를 바꿀 때와 창을 닫을 때 모두 확인이 필요합니다.

```js
window.addEventListener('beforeunload', (e) => {
    if (attChanges().length > 0) { e.preventDefault(); e.returnValue = ''; }
});
```

---

## 검증

1. 두 명 찍고 저장 → **바뀐 두 명만** 전송되는가 (네트워크 탭 확인)
2. `◎` 인 줄에 O·X 버튼이 **아예 없는가** (배지만 보여야 한다)
3. `◎` 인 사람이 있는 조에서 `전체 출석`·`빈칸 → 결석`·`전체 지우기` →
   **`◎` 가 하나도 안 바뀌는가**
3. 저장 성공 후 같은 버튼을 다시 눌러 빈칸으로 → 저장 → **기록이 지워지는가**
4. 캐시가 있는 상태로 화면을 열고 배경 갱신을 기다림 → **새 값으로 바뀌는가**
5. 편집 중에 배경 갱신이 오면 → **입력이 유지되고 경고가 뜨는가**
6. 수업 전 주차를 열었을 때 → **결석 0, 미기록 N** 으로 나오는가
7. 주차를 바꿀 때 저장 안 한 변경이 있으면 → **묻는가**
8. 조를 `전체` 로 두면 → 전원이 **조별로 묶여** 나오는가

---
