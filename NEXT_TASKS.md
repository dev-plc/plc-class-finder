# 작업 내역 · 남은 일

마지막 갱신: 2026-08-22 (v105)

## 목표

행정력 최소화. "관리자가 시트를 눈으로 훑어 대상자를 찾아내는" 일을 없앤다.
자동 판정 · 자동 리포트 · 당사자 자가 확인.

## 원칙

- **입력은 익숙한 도구로, 조회·판단은 DB로**
  - 편성 → 시트에서 (드래그·복사가 웹 UI보다 빠름)
  - 김밥·과제 신청 → 구글 폼에서
  - 출석 체크 → 웹앱에서
- **출결의 원본은 시트다. DB 는 사본이다** ← 2026-08 에 뒤집었다
- **규칙은 SQL 뷰 한 곳에만** → 기준이 바뀌면 한 줄만 고친다

> 한때 DB 를 원본으로 두고 앱이 직접 썼다. 시트에서도 손으로 고치니 두 곳에서
> 쓰는 꼴이 됐고, 어느 쪽이 최신인지 판단할 근거가 없어 어긋났다.
> 시트에만 있던 ◎ 를 앱이 몰라 이수자가 결석으로 저장된 일이 실제로 있었다.
> 지금 흐름은 `CLAUDE.md` 에 있다.

---

## 확정된 규칙

```
분모: 실제 강의 16강 = 교리1~12 + 성경적대화1~4
      (교제·나눔은 is_class=false 로 제외)

출석 인정:  O (현장)  ·  ◎ (지난 기수 이수 이월)
결석:       X  ·  과제 (결석했지만 과제·소감문으로 메움)
집계 제외:  −  (하차·중도합류·휴강 등 사유 불문)
            빈칸 (아직 기록되지 않음 — 결석이 아니다)

보충: 결석(X·과제) 주차에 과제·소감문 제출 시 출석 인정, 최대 3회
      '과제' 는 사람이 읽으라고 붙이는 라벨이고, 인정 근거는 제출 기록이다

수료(O)      : credited(= present + makeup_used) >= 16
관리자확인   : 보충이 3회를 초과 (참작 사유 판단 필요)
미수료(X)    : 그 외

재수강       : 4회 결석 (새가족교육 안내문 4항)
△(부분수료)  : 기준 미확정. 시트 값만 보존하고 자동 판정에는 미사용
```

**규칙은 `supabase/views.sql` 한 곳에만 있다.**

---

## 완료

### 1~3단계 — 기반 · 판정 · 본인 안내  ✅

- 스키마·동기화·GitHub Actions (`sync-db` / `query-db` / `run-sql`)
- 판정 뷰 7종 (`v_completion_status` 외)
- 조회 화면의 진행률 카드 · 미제출 과제 안내

### 4단계 — 튜터·관리자 도구  ✅

| 화면 | 내용 |
|---|---|
| **출석 관리** | 주차·조 선택 → `O X` 입력 → 바뀐 사람만 저장. `◎ −` 는 보기 전용 |
| **결석 현황** | 이 주차 / 누적(2·3·4회 이상). 과제·소감문 대체분도 표시 · 담당교역자별 · 정렬 · 명단 복사 |
| **수료 현황** | 여섯 갈래 + 갈래 안 재분류(예정·진행 중은 '몇 회 더', 아슬아슬은 '과제 몇 건') · 할 일까지 |
| **출석부 출력** | 조별 A4 인쇄. 칸 토글 · 월말 김밥신청 자동 · 장별 출력 선택 · 집계표 |
| **개인별 보기** | 카드를 누르면 출석 그리드(과제·김밥 표시) · 김밥 · 과제 |
| **조별 보기** | 편성 확인 |
| **시트에서 지금 가져오기** | 관리자 화면 버튼 → GAS → GitHub Actions 동기화 |

### 4.5단계 — 기수 전환  ✅

`cohorts.is_active` 한 줄로 전환. 절차는 `COHORT_SWITCH.md`.
지난 기수 이수분(◎) 자동 이월 — `scripts/carry-over-attendance.mjs`.

### B안(DB 원본) 되돌리기  ✅

- 앱의 쓰기는 `sbPostGas` 로만 나간다. `sbRpc` 는 일부러 두지 않았다 —
  직접 쓰는 길을 열어 두면 언젠가 누군가 그 길로 간다
- GAS `doPost` → ① 출석부 탭(원본) ② `set_attendance_batch`(사본)
- 시트에 직접 입력한 것은 10분 트리거(`pushAttendanceToDb`)가 DB 로 밀어 올린다
- 그래서 **DB → 시트 역동기화는 필요 없어졌다** (예전 숙제였다)
- `pullAttendanceFromDb`(DB → 시트)는 **기수 시작 때만**. 평소에 돌리면 원본을 덮는다

### 5단계 — 튜터가 스스로 챙긴다  ✅

조원 명단 위 한 줄. 평소엔 숫자만, 누르면 챙길 사람과 **할 일**만 펼친다.

```
우리 조 7명   수료 0   예정 2   진행 중 2   기록 없음 1     [챙길 사람 3 ▾]
'예정'은 앞으로 3회 안에 채워지는 사람입니다. 남은 강의는 10회.
```

- 전체 출석표 이름 옆에 진행률(`12/16`) — 갈래별 색, 이름 칸과 함께 고정
- 튜터 화면은 **판정을 통보하지 않는다.** 산술적으로 어려운 사람에게도
  '담당 교역자와 의논이 필요합니다' 라고만 적는다. 사정을 아는 것도
  예외를 판단하는 것도 교역자 몫이고, 그 말을 튜터가 전하게 두면 안 된다
- 계산은 `getCompletionOutlook` 한 곳에만 있다 (관리자 탭도 같은 함수)

---

## 2026-08 에 잡은 것들

| 무엇 | 왜 위험했나 |
|---|---|
| 출결 원본을 시트로 되돌림 | 두 곳에서 쓰다가 이수자(◎)가 결석으로 덮였다 |
| `◎ −` 를 입력 불가로 | 확인 대화상자는 사람들이 그냥 누른다. 구조로 막았다 |
| 빈칸이 결석으로 저장되지 않던 문제 | 튜터가 출석자만 체크하면 결석이 하나도 기록되지 않았다 |
| 결석 집계를 기준 주차까지만 | 지난 기수 참석자의 앞 주차 X 가 잡혀 무결석자가 4회로 떴다 |
| 명단 순서를 시트와 일치 | 종이·화면·시트가 제각각이라 옮겨 적다 사람을 잘못 짚었다 |
| 인쇄 시 빈 종이 | `.admin-container` 여백 5mm 가 마지막 장을 밀어냈다 |
| 안내방 라벨이 첫 조회자에게 고정 | 행을 재사용하면서 라벨을 만들 때만 넣었다 |
| 온라인 조에 현장 안내방 | 방 이름을 짐작해 적었다. 띄어쓰기 무시 비교로 고쳤다 |
| 버전 표시를 손으로 적던 것 | `script.js v62` 에 멈춘 걸 v72 에서야 발견했다. 이제 URL 에서 읽는다 |
| 엣지 캐시가 HTML 을 붙듦 | 배포는 됐는데 몇 주 전 화면이 나왔다 (`HOSTING_REQUEST.md`) |
| 아이디 정형화 | 과제 탭 아이디에 괄호·하이픈이 섞여 명단과 짝이 안 맞았다 |
| ◎ 를 '지난 기수 이수' 로만 적던 것 | 결석 후 과제·소감문 대체도 ◎ 다. 화면이 절반만 말하고 있었다 |
| 과제 링크 두 개가 한 칸에 | 파일을 둘 내면 폼이 `", "` 로 이어 붙인다. 그대로 열면 둘 다 안 열렸다 |
| 일 1회 동기화가 조용히 실패 | 8/21 정오 실행이 GAS 404 로 죽었다. 재시도도 알림도 없어 하루가 갔다 |
| 미기록을 '영영 못 채우는 것' 으로 셈 | 앞 주차를 안 찍어 둔 조에서 무결석자가 0/16 · 수료 불가로 떴다 |
| 빈칸의 뜻이 셋인데 범례에 둘만 | '·'(미기록)이 범례에 없어 '수업없음' 으로 읽혔다. 데이터는 멀쩡했다 |
| '수료 예정' 이 초반에 전원 | 결석만 없으면 다 예정이었다. 3회 안에 채울 사람만 예정으로 좁혔다 |
| 대체한 사람이 결석 명단에서 사라짐 | 규정대로 과제를 낸 사람이 안 보여 관리자가 아예 못 봤다. 대체 표시를 달아 함께 띄운다 |
| `◎` 로 적으면 3회 한도가 우회됨 | present 로 세어져 makeup_limit 을 안 거쳤다. `과제` 를 따로 두고 결석으로 센다 |
| 조 순서를 이름으로 짐작 | 'V3' 조가 맨 앞에 왔다. DB 는 라틴을 한글보다 앞세우고, 조별보기 접두 표는 모르는 이름을 -1 로 줬다 |
| 한 장짜리 인쇄가 종이 가운데 | style.css 의 body flex 중앙정렬을 body.printing 이 안 풀었다. 두 장 이상이면 넘쳐서 안 보였다 |
| ⟳ 를 눌러도 화면이 그대로 | GAS 는 실행을 '요청' 만 하고 돌아온다. 23~46초 뒤 사람이 한 번 더 눌러야 했고, 언제 끝났는지 알 길이 없었다 |
| 날짜 없는 과제를 버리던 것 | 수기 제출을 담당자가 옮겨 적은 정상 데이터였다. 통째로 떨어져 보충 인정이 안 됐고 건수는 로그에만 남았다 |
| 조 카드 대표 교역자가 늘 빈칸 | 화면은 `m.pastor` 를 읽는데 데이터 계층은 `'담당교역자'` 키로 넣었다. 조장 판정도 시트에 없는 '조장' 을 찾고 있었다 |
| 매트릭스 코드가 두 벌 | script.js 와 admin.js 가 세션명 정규화를 각자 이름으로 갖고 있었다. 한쪽만 고쳐지면 한참 뒤에 드러난다 |
| 튜터에게 본인 수료 진행률 | 튜터는 수강생이 아닌데 '수료까지 12회' 가 떴다. 자기가 대상인 줄 안다 |
| 전체 출석표가 가로로 무한정 | 16주가 되면 정작 볼 이번 주가 화면 밖으로 나간다. 최근 10주 + 다가오는 한 주만 그린다 |
| ⟳ '시트에서 지금 가져오기' 가 출석을 안 가져옴 | 워크플로는 명단·편성·위치·과제제출·김밥만 본다. 출결은 pushAttendanceToDb 한 길뿐이라, 시트를 고치고 눌러도 화면이 그대로였다. 버튼 이름 탓에 아무도 의심하지 않았다 |
| GAS 버전을 안 올리고 값만 바꿈 | '과제' 를 넣고도 v30 그대로라 편집기의 코드가 옛것인지 새것인지 응답으로 가릴 수 없었다. 옛 코드가 배포된 채 한참 갔다 |
| 시트 자동화가 보충을 `◎` 로 찍고 있었음 | 과제+소감문을 내면 X 를 `◎` 로 바꾸는 스크립트가 시트에 따로 살아 있었다. `◎` 는 present 로 세어져 3회 한도를 통째로 우회한다 — `◎` 가 두 뜻을 겸하던 근원이 이것이었다 |
| 보충 인정 기준이 시트와 DB 에서 달랐음 | 시트는 '과제+소감문' 만 라벨을 붙이는데 DB 는 유형을 안 봐서 아무 제출이나 인정했다. 화면에는 '과제+소감문 대체' 로 뜨는데 실제로는 과제만 낸 사람이 있었다 |
| `pushAttendanceToDb` 가 아이디를 공백만 지우고 맞춤 | v30 이 `plcNormalizeId_` 로 통일했는데 이 파일만 빠져 있었다. DB 아이디는 name+phone 이라 기호가 없으므로, 시트에 `김도현-5326` 처럼 적히면 짝이 안 맞아 **그 사람 출결이 통째로 안 올라간다.** 오류도 로그도 없었다 |
| 편집기에서 GAS 미리보기가 멈춰 있던 것 | `getUi().alert()` 이 띄울 자리를 기다린다. 무한 루프처럼 보인다 — 로그를 먼저 남기게 고쳤다 |
| **GAS 프로젝트가 둘이라는 것을 몰랐음** | 웹앱(`doGet`/`doPost`)은 독립 프로젝트, `pullAttendance.js`·`homeworkToAttendance.js` 는 시트에 붙은 프로젝트다. **전역을 공유하지 않는다.** ⟳ 가 `pushAttendanceToDb` 를 부르게 만든 v32 가 그래서 안 돌았다 (v33 에서 물림). 출석은 10분 트리거가 계속 가져온다 |

## 2026-09 에 잡은 것들

| 무엇 | 왜 위험/불편했나 |
|---|---|
| 미래 주차의 명시적 결석 누락 | 지난 기수 보충을 위해 미래 주차에 결석(X)을 미리 기록해도 화면에 '예정'으로 덮어써지고 즉시 과제 안내 대상에서 제외되었다 |
---

## 남은 일

### 지금 사람이 해야 하는 것

- [x] **`과제` 값 켜기 (v104)** — **2026-09-03 완료.**
      `views.sql` · `rpc_attendance.sql` 재실행 + GAS 두 파일(`PLC_ALLOWED_STATUS` ·
      `PLC_PUSH_ALLOWED`) + 웹앱 재배포까지 끝났다. 시트의 `과제` 가 앱에 노란
      `과제` 로 뜨는 것을 확인했다.
      배운 것: 실제로 화면을 바꾸는 것은 `pullAttendance.js` 의 `PLC_PUSH_ALLOWED`
      다 — `doGet.js` 쪽은 앱이 쓸 때만 쓰이는데 앱은 `과제` 를 안 보낸다.
- [x] **GAS v32** — **2026-09-03 배포 확인.** ⟳ 가 `pushAttendanceToDb` 를 먼저
      돌려 출석까지 가져온다. 응답 `version` 이 **32** 면 최신이다.
- [ ] **`synced_at` 켜기 (v106)** — 동기화가 끝난 것을 화면이 알아채는 칸
      (SQL 은 2026-09-03 에 실행했다고 들었으나 화면 확인은 아직이다)
  1. Supabase 에서 `supabase/add_synced_at.sql` 실행 (`alter table` 한 줄)
  2. **켜졌는지 확인**: 동기화 바에 `마지막 동기화: N분 전` 이 뜨면 된 것이다.
     26시간이 넘으면 빨갛게 — 8/21 처럼 크론이 조용히 죽은 것을 화면에서 안다
  3. 그 전까지는 ⟳ 를 누르면 60초 뒤 한 번 새로 그리는 폴백으로 돈다.
     그때 뜨는 `새로 그렸습니다. (동기화가 그 사이 끝났는지는 확인하지 못했습니다)`
     가 곧 '아직 안 켜졌다' 는 표시다. 칸이 생기면 5초마다 확인해 끝나는 즉시 그린다
- [x] **`sheet_row` 켜기 (v105)** — 조 순서를 시트대로. **2026-09-02 확인 완료**
      (V3 가 맨 뒤로 갔다). SQL 실행 + 동기화로 값이 채워졌다
- [x] **보충 인정을 '과제+소감문' 으로 조이기 + 옛 `◎` 가르기** — **2026-09-03 완료.**
      `views.sql` 재실행 + 시트 스크립트 교체 + `syncAllAttendance` 로 **17칸**
      (`◎`→`과제` 17 · `X`→`과제` 0)을 바꿨다.

  | 잰 것 | 결과 |
  |---|---|
  | 유형 분포 | `과제` 208 · `과제+소감문` 25 (빈 값 없음) |
  | 그중 결석 주차 | 제출없음 36 · `과제` 3 · `과제`+`과제+소감문` 1 |
  | 유형 조이기 영향 | 허홍범 11→10 · 한보연 13→12 · 서미화 15→14, `verdict` 변화 없음 |
  | `◎`→`과제` 영향 | **아무도 안 움직임** — 최다가 3칸이라 `least(3,3)=3` 으로 값이 같다 |

  208건이라는 숫자만 보면 대규모 변경 같지만 거의 다 출석(O)한 주차 것이라
  보충과 무관했다. **큰 숫자에 놀라기 전에 결석 주차만 추려 보는 것**이 요령이다.

  미리보기의 '짝을 못 찾은 제출 200건' 도 정상이다 — 과제 탭이 기수를 넘어
  쌓이고 지난 기수 사람은 지금 출석부에 없다. 3기 과제+소감문은 DB 기준 25건,
  시트 쪽 매칭도 25건으로 정확히 맞았다.

- [x] **GAS 웹앱 재배포** — `scripts/gas/doGet.js` v30 배포 완료 (2026-08-22).
      다음에 `doPost` 를 고치면 또 재배포해야 한다. 배포 관리에서 기존 배포의
      **버전만** 올린다 (새 배포를 만들면 URL 이 바뀐다)
- [ ] **GAS 스크립트 속성 `GH_TOKEN`** — 동기화 버튼용 fine-grained PAT
      (`Actions: Read and write`)
- [ ] **git 이력의 개인정보** — 지웠던 CSV 5개가 이력에는 남아 있다.
      저장소가 public 인 동안 이름·전화 뒷4자리가 계속 꺼내진다.
      저장소를 private 으로 돌리거나 `git filter-repo` 로 잘라내야 한다
- [ ] **원격 브랜치 `claude/fix-attendance-cache-sKSlV`** — `main` 과 공통 조상이 없는
      별도 이력(8/8까지). 필요 없으면 GitHub 화면에서 삭제

### 기능

- [ ] **김밥 신청 배너** — 매월 마지막 주에 앱에 노출, 구글 폼으로 연결
  - ⚠️ 김밥 전용 폼이 아직 없다 → 새로 만들어야 함 (관리자 작업)
- [ ] **신규 신청자 목록** — 교육신청 폼으로 들어온 미편성 인원 알림
- [ ] **조별 리포트** — 튜터별 대상자 요약
      (결석 현황 · 수료 현황 · 튜터 화면 한 줄이 대신하고 있다.
       남은 건 텔레그램으로 밀어 주는 것뿐)
- [ ] **텔레그램 봇 주간 리포트** — 조별 방에 자동 게시 (봇 토큰 필요)

---

## 미결정

- **`−`(수업 없음)가 있는 사람의 수료 분모** — 지금은 16 고정이라
  `−` 가 하나만 있어도 최대 도달치가 15가 되어 수료가 불가능하다.
  중도합류자가 여기 걸린다. `required = 16 − skipped_count` 로 바꿀지는 보류.
  수료 현황 탭에서 '수료 불가' 로 뜨므로 눈에는 띈다
- **언제 시작했는지 표시** — 첫 O 자리로 잡아 봤으나(v101~102) 되돌렸다.
  ◎ 의 두 뜻 때문이었는데 v104 에서 `과제` 를 갈라내 **막힌 이유가 없어졌다.**
  값 옮기기(아래)가 끝나면 다시 볼 만하다
- `△`(부분수료) 기준 — 확정되면 `v_completion_status` 에 반영
- 김밥 신청 폼 항목 구성
- 리포트 발송 주기·시각

---

## 현재 상태 (2026-08 · 3기)

- **활성 기수 3기** — 강의 16개(교리1~12 + 성경적대화1~4) · 비강의 3개
- **2기 보존** — 142명 · 수료 46 / 관리자확인 1 / 미수료 95
- **이월 완료** — 2기 이수분 140칸을 3기에 ◎ 로 기록.
  과제로 인정받은 주차 15칸은 X → ◎ 로 바꿨다

배포는 `main` → GitHub Pages → `classfinder.plch.kr`.
프론트를 고치면 **버전 네 자리를 같은 값으로** 올린다 (`CLAUDE.md` 참고).
관리자 화면 오른쪽 위 배지에 지금 도는 버전이 뜬다 — 고친 게 안 보이면 거기부터 본다.
