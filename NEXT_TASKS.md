# Phase C — DB 원본 전환 + 행정 자동화

## 목표

행정력 최소화. "관리자가 시트를 눈으로 훑어 대상자를 찾아내는" 일을 없앤다.
DB 전환은 수단이고, 목표는 자동 판정 · 자동 리포트 · 당사자 자가 확인이다.

## 원칙

- **입력은 익숙한 도구로, 조회·판단은 DB로**
  - 편성 → 시트에서 (드래그·복사가 웹 UI보다 빠름)
  - 김밥·과제 신청 → 구글 폼에서
  - 출석 체크 → 웹앱에서 (이미 구현됨)
- **웹앱은 DB만 읽는다** → 진실은 하나
- **규칙은 SQL 뷰 한 곳에만** → 기준 변경 시 한 줄만 수정

---

## 확정된 수료 규칙

```
분모: 실제 강의 16강 = 교리1~12 + 성경적대화1~4
      (교제·나눔은 is_class=false 로 제외)

출석 인정:  O (현장)  ·  ◎ (지난 기수 이수분)
결석:       X
집계 제외:  -  (하차·중도합류·휴강 등 사유 불문)
            빈칸 (아직 기록되지 않음)

보충: 결석(X)한 주차에 과제·소감문 제출 시 출석 인정, 최대 3회
      인정 건수 makeup_used, 상세는 v_makeup_detail

수료(O)      : credited(= present + makeup_used) >= 16
관리자확인   : 보충이 3회를 초과 (참작 사유 판단 필요)
미수료(X)    : 그 외

△(부분수료): 기준 미확정. 시트 값만 보존하고 자동 판정에는 미사용
```

**규칙은 `supabase/views.sql` 한 곳에만 있다.** 기준이 바뀌면 이 파일만 고친다.

---

## 작업 순서

### 1단계 — 기반  ✅ 완료

- [x] 스키마 보강 (`supabase/schema_v2.sql`)
      sessions.label_norm·is_class / members.status·created_at·admin_note
      kimbap_signups / homework_submissions
- [x] 동기화 (`scripts/sync-sheet-to-db.mjs`)
      GAS v21 응답 → Supabase 전량 upsert
      시트에서 빠진 인원은 status=inactive (이력 보존)
      기수 시작 연도는 cohorts.started_at 에서 읽음
- [x] GitHub Actions
      sync-db.yml   수동 + 일 1회 (Supabase pause 방지 겸용)
      query-db.yml  preset 조회
      run-sql.yml   임의 SELECT (진단용, exec_sql RPC 경유)

### 2단계 — 판정 규칙  ✅ 완료

- [x] v_attendance_summary  출석·결석·보충 집계
- [x] v_completion_status   수료/관리자확인/미수료
- [x] v_homework_required   결석(X) 주차 중 미제출 건만
- [x] v_completion_risk     남은 강의로도 도달 불가한 인원
- [x] v_team_report         조별 요약
- [x] v_makeup_detail       보충 인정 건별 내역
- [x] v_recent_members / v_inactive_members

검증 (2026-07-31 기준 142명):
  수료 41 · 관리자확인 1 · 미수료 100
  present + makeup_used = credited 전 행 일치, credited <= 16 확인

### 3단계 — 본인 안내  ✅ 완료

당사자가 스스로 알면 관리자가 찾아 알릴 일이 사라진다.

- [x] 조회 결과 최상단에 진행률 카드 (`#myStatusCard`)
      `14 / 16강 이수 · 수료까지 2회` + 진행률 막대 + 출석/결석/과제인정
- [x] 결석 주차 중 미제출 건만 노출 (출석한 주차는 제외) + 제출 폼 링크
- [x] 보충 3회 초과 시 "담당자 확인이 필요합니다" 로 문구 전환
- [x] 수료자는 과제 블록을 숨기고 축하 문구만

판정은 전부 `v_completion_status` · `v_homework_required` 에서 읽는다.
앱은 표시만 하고 규칙을 갖지 않는다.

### 4단계 — 튜터·관리자 도구

- [x] **관리자 출석 화면** (`admin.html` → 출석 관리 탭, 기본 탭)
      주차 · 조 선택 → `O ◎ X −` 4단 상태 → 바뀐 사람만 일괄 저장
      - 미기록(주황 띠) · 미저장 변경(파란 띠) 을 눈으로 구분
      - 전체 출석 / 빈칸→결석 / 전체 지우기 / 되돌리기
      - 선택한 주차·조는 localStorage 에 기억
      - 저장 직후 판정 뷰만 다시 읽어 진행률을 맞춘다 (`refreshProgress`)
      - ⚠️ `supabase/rpc_attendance.sql` 을 실행해야 저장이 동작한다
- [ ] **조별 리포트** — 튜터별 대상자 목록
  ```
  📋 새A조 — 7/26 기준
  ⚠️ 수료 위험 2명 / 📝 과제 미제출 3명 / 🍙 이번 주 김밥 4명
  ```
- [ ] **출석부 생성** — 인쇄·공유용 출력물
  - 조별 · 전체 두 형태
  - 기존 `attendance_sheet.html` 참고
- [ ] **관리자 확인 목록** — 보충 3회 초과 등 재량 판단 필요 건

### 4.5단계 — 기수 전환  ✅ 완료

기수를 코드에 박아두면 3기·4기마다 배포를 해야 한다. DB 한 줄로 끝나게 했다.

- [x] 앱이 `cohorts.is_active` 를 따라감 (`supabase-config.getActiveCohortId`)
      캐시에 기수를 함께 저장하고, 바뀌면 이전 기수 화면을 걷어내고 알린다
- [x] 동기화도 활성 기수 자동 추적. `--activate` 로만 활성 기수를 바꾼다
      (매일 도는 cron 이 활성 기수를 되돌리면 안 되므로)
- [x] 출석 조회를 활성 기수 인원으로 한정 (기수가 쌓여도 전량 조회하지 않음)
- [x] GAS 시트 ID·탭 이름을 상단 상수로 분리 (v22)
- [x] **지난 기수 이수분(◎) 자동 이월** — `scripts/carry-over-attendance.mjs`
      지난 기수에 들은 주차를 새 기수 출석부에 ◎ 로 찍는다.
      기본 dry-run, 빈칸만 채우고 기존 기록은 덮지 않는다
- [x] 절차서: `COHORT_SWITCH.md`

### 5단계 — 운영 편의

- [ ] **김밥 신청 배너** — 매월 마지막 주에 앱에 노출, 구글 폼으로 연결
  - ⚠️ 김밥 전용 폼이 아직 없음 → 새로 만들어야 함 (관리자 작업)
  - 폼 응답 → 시트 → 동기화 → DB 경로
- [ ] **신규 신청자 목록** — 교육신청 폼으로 들어온 미편성 인원 표시
  - 편성 자체는 시트에서, 앱은 "누가 새로 왔는지" 알림만
- [ ] **수료·하차 처리** — 시트에서 상태 변경 후 동기화

### 6단계 — 자동 발송 (선택)

- [ ] 텔레그램 봇 → 조별 방에 주간 리포트 자동 게시
  - 이미 조별 텔레그램 방이 있으므로 자연스러움
  - 봇 토큰 발급 필요

---

## B안(출석은 DB가 원본) 전환에 따라 남은 것

- [ ] **DB → 시트 역동기화**
      앱에서 기록한 출석이 시트에 보이지 않는다.
      시트를 계속 보실 거라면 Actions 에서 DB → 시트 방향 쓰기가 필요하다.
      (읽기 전용 사본 탭으로 내려받는 쪽이 충돌이 없다)
- [ ] 시트 출석 열은 **읽기 전용 안내** 를 달아 두는 편이 안전하다.
      양쪽에서 고치면 동기화 방향이 무너진다.

## 미결정 / 확인 필요

- `△`(부분수료) 기준 — 확정되면 `v_completion_status`에 반영
- 김밥 신청 폼 — 신규 제작 필요. 항목 구성 협의 필요
- 리포트 발송 주기·시각 — 주 1회? 수업 직후?

---

## 참고: 현재 상태 (2026-08-01)

- 웹앱: Supabase 전환 완료 (`members-data v32`). GAS 는 동기화 경로에만 남았다
- 출석 쓰기: `set_attendance_batch` RPC 한 곳으로만 (테이블 직접 쓰기는 막혀 있음)
- 김밥·과제: 시트 → GAS → Actions 동기화 → DB 테이블
- 팬텀 컬럼(07/19, 07/22) 시트에서 삭제 완료
