# DGfinder 인계 — 관리자 페이지의 조별 출석부 출력

DGfinder Code 대화에 **아래 `---` 사이를 통째로** 붙여넣으세요.
실제 구현은 `plc-class-finder` 의 `admin.js` (출석부 출력 절) · `admin.html` · `admin.css` 입니다.

---

# 작업: 조별 출석부를 앱에서 뽑는다

## 무엇을 만드나

지금까지 관리자가 스프레드시트로 만들어 인쇄하던 **조별 출석부**를 앱에서 냅니다.
현장에서 손으로 체크하는 종이 양식입니다.

```
주차 [08/16 · 교리2 ▾]        조·장소 [전체 (12개 조) ▾]
표시할 칸  (김밥 현황) (김밥신청) (과제) (메모) (조별 집계표)
[🖨️ 인쇄] [전체 선택] [전체 해제]              13장 중 11장 출력

                                              ☑ 출력
┌──────────────────────────────────────────────────┐
│ 새A                                    08/16 교리2│
│ ─────────────────────────────────────────────────│
│ 웨슬리 · 인원 12명 · 김밥 3명                     │
│ ┌────┬──────────┬────┬──────┬────┬──────────────┐│
│ │No. │ 이름     │김밥│김밥신청│출석│1 □ ◎ 메모 ▷ ♡ 5││
│ ├────┼──────────┼────┼──────┼────┼──────────────┤│
│ │ 1  │박민수5326│ O  │      │    │              ││
│ │ 2  │이성은9064│    │      │    │              ││
```

**아는 값은 미리 찍고, 받을 값은 비웁니다.**

| 칸 | 채우나 | 왜 |
|---|---|---|
| 번호·이름 | 데이터 | |
| 김밥 | 데이터 | 이미 신청한 사람 표시 |
| 과제 | 데이터 | 제출자에게 `✓` |
| 출석 | **빈칸** | 현장에서 손으로 |
| 김밥신청 | **빈칸** | 다음 달 신청을 받는다 |
| 메모 | **빈칸** | |

## 칸을 켜고 끈다

체크박스로 각 칸을 켜고 끕니다. **한 체크박스가 두 칸을 제어하면 안 됩니다** —
`김밥 현황`(데이터)과 `김밥신청`(빈칸)을 묶었다가, 신청 칸을 끄면 현황까지 사라졌습니다.
성격이 다르면 따로 두세요.

**`김밥신청` 은 그 주차가 해당 월의 마지막 수업이면 자동으로 켜집니다.**
다음 달 김밥을 그때 받기 때문입니다.

```js
function isLastClassOfMonth(sessionDate) {
    const ym = String(sessionDate).slice(0, 7);
    const same = getSessions()
        .filter(x => String(x.session_date).slice(0, 7) === ym)
        .map(x => x.session_date).sort();
    return same.length > 0 && same[same.length - 1] === sessionDate;
}
```

**사람이 직접 토글하면 그 뜻을 존중하세요.** 자동 판단이 사람 판단을 덮으면 신뢰를 잃습니다.

## 칸 선택을 기억한다 — 단, 김밥신청은 주차별로

과제 칸을 쓰는 사람은 매번 씁니다. 들어올 때마다 다시 체크하게 두면 안 됩니다.
`김밥 현황`·`과제`·`메모`·`집계표` 는 그냥 `localStorage` 에 저장하면 됩니다.

**김밥신청만 다릅니다.** 그건 취향이 아니라 '월 마지막 수업이냐' 로 정해지는 값입니다.
다른 칸처럼 저장해 버리면 **한 번 끈 순간 자동 판단이 영영 죽습니다** — 다음 달
마지막 주에도 칸이 안 나옵니다. 그래서 **주차별로** 기억하세요.

```js
let kimbapBy = {};   // { '2026-08-16': true|false }

// 사람이 말한 적 없는 주차 → 자동, 정한 적 있는 주차 → 그 뜻대로
const resolved = typeof kimbapBy[date] === 'boolean'
    ? kimbapBy[date]
    : isLastClassOfMonth(date);
```

이러면 둘이 안 부딪힙니다. 자동은 빈자리에서만 말하고, 사람 말은 안 덮입니다.
주차 기록은 기수가 바뀌어도 쌓이기만 하니 최근 40개 정도만 남기세요.

**왜 켜졌는지 한 줄로 알려주세요.** 자동으로 켠 건지 내가 정해둔 값인지
구분이 안 되면 사람이 앱을 못 믿습니다 — "월 마지막 수업이라 자동으로 켰습니다" 와
"끈 것으로 기억하고 있습니다" 는 다른 말입니다.

## 범위 선택

조 드롭다운에 **장소별 묶음**을 넣으세요. 웨슬리홀만, 칼빈채플만 뽑는 일이 잦습니다.

```
전체 (12개 조)
── 장소별 ──
  웨슬리 (9개 조)
  온라인 (3개 조)
── 조별 ──
  새A (12명)  …
```

**장소가 한 곳뿐이면 묶음을 숨기세요.** 고를 것이 없는 목록은 방해만 됩니다.

## 장마다 출력 체크

각 장 위에 `출력` 체크를 답니다. **기본은 전부 켬** — 전체를 뽑되 몇 조만 빼는 것이 실제 쓰임입니다.

**체크를 눌러도 다시 그리지 마세요.** 다시 그리면 다른 장의 체크가 전부 초기화됩니다.
클래스만 토글하세요. 다만 **주차나 조를 바꾸면 제외 목록을 비웁니다** —
범위가 달라지면 뺐던 조가 목록에 없을 수도 있어 그 선택은 뜻을 잃습니다.

## A4 한 장을 채운다

조마다 인원이 달라(12명 ~ 1명) 고정 줄 높이로는 아래가 휑하거나 넘칩니다.
**남는 높이를 인원수로 나누되 위아래로 묶으세요.**

```js
// A4 세로 297 - 위아래 여백 24 = 273mm
// 제목·주차·인원 18mm + 표 머리글 8mm + 여유 4mm = 30mm
const AVAIL = 273 - 30;

function rowHeightMm(count) {
    if (!count) return 9;
    return Math.round(Math.min(22, Math.max(9, AVAIL / count)) * 10) / 10;
}
```

12명이면 20.3mm 로 99%, 22명이면 11mm 로 98% 찹니다.
**상한 22mm 가 필요합니다** — 5명짜리 조를 꽉 채우려면 한 줄이 40mm 가 되는데,
그건 표가 아니라 빈 상자입니다. 인원이 적은 조는 아래가 남는 게 맞습니다.

`<section style="--pr-row: 20.3mm">` 로 실어 보내고 CSS 는 `height: var(--pr-row)` 로 받습니다.

---

# ⚠️ 함정 — 전부 실제로 밟았습니다

## 1. `table-layout: fixed` 를 안 쓰면 남는 폭이 아무 칸에나 붙는다

메모 칸을 끄는 순간 체크 칸들이 쓸데없이 넓어지고, 이름 칸이 300px 넘게 벌어집니다.

```css
.pr-table { width: 100%; table-layout: fixed; }
.pr-table .pr-c-no   { width: 26px; }
.pr-table .pr-left   { width: 122px; }   /* 이름 4자 + 전화 4자리 */
.pr-table .pr-c-mark { width: 34px; }    /* 손으로 O 하나 */
.pr-table .pr-c-wide { width: 64px; }    /* '김밥신청' 은 글자가 길다 */
.pr-table .pr-c-memo { width: auto; }    /* 남는 폭은 여기가 먹는다 */
```

**메모를 끄면 테두리 없는 채움 칸을 대신 넣으세요.** 안 그러면 `fixed` 의 성질상
모자란 폭만큼 나머지 칸이 비례해서 늘어납니다.

## 2. `print-color-adjust: exact` 가 없으면 배경이 종이에 안 찍힌다

브라우저는 기본적으로 배경색을 인쇄하지 않습니다. **표 머리글 음영도, 줄 구분도,
합계 줄 강조도 하나도 안 나옵니다.** 화면에서만 예쁘고 인쇄물은 밋밋해집니다.

```css
@media print {
    .pr-page { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
}
```

## 3. `:last-child` 로 마지막 장을 잡으면 빈 종이가 나온다

조마다 `page-break-after: always` 를 걸면 마지막 장 뒤에도 새 장이 생깁니다.
그래서 마지막 장만 예외로 두는데, **뺀 장은 숨겨질 뿐 DOM 상으로는 여전히 마지막일 수 있습니다.**
인쇄 직전에 **실제로 인쇄되는 마지막 장**을 찾아 표시하세요.

```js
const live = [...preview.querySelectorAll('.pr-sheet:not(.pr-skip)')];
if (!live.length) { alert('출력할 장이 없습니다.'); return; }
preview.querySelectorAll('.pr-last').forEach(el => el.classList.remove('pr-last'));
live[live.length - 1].classList.add('pr-last');
```

## 4. 체크박스를 종이 위에 얹으면 잘린다

미리보기 컨테이너가 가로 스크롤(`overflow-x: auto`)을 쓰면 **밖으로 삐져나온 것을 잘라냅니다.**
종이 모서리에 걸치게 두면 체크가 반쯤 잘려 보입니다.
**종이 위가 아니라 종이 밖 상자에 올리세요.**

```html
<div class="pr-sheet">          <!-- 제외·마지막 장 판정은 이 상자 기준 -->
  <label class="pr-pick">…</label>
  <section class="pr-page">…</section>
</div>
```

## 5. 미리보기를 A4 비율로 두면 인쇄 전에 판단할 수 있다

이게 가장 도움이 됩니다. 화면에서 보이는 여백이 곧 인쇄물의 여백이 되니
**종이로 뽑아보지 않고도** 아래가 휑한지, 표가 넘치는지 압니다.

```css
.pr-page { width: 186mm; min-height: 273mm; }   /* 210-24 × 297-24 */
```

## 6. 조작부를 스크롤에 붙여라

조가 13개면 미리보기가 13장입니다. 조를 빼거나 칸을 바꾸려면 매번 맨 위로
올라가야 했습니다. `position: sticky; top: 0` 한 줄이면 됩니다.

## 7. 과제 세션명은 정규화한 뒤 비교하라

폼 응답의 세션명과 시트의 강의명이 다르게 적힙니다 (`13강` / `성경적대화1`, `교제` / `교재`).
그대로 비교하면 **과제 `✓` 가 한 번도 안 뜨는데 오류가 안 나서** 알아채기 어렵습니다.
양쪽을 같은 규칙으로 정규화하세요.

---

## 검증

1. 칸 체크박스를 하나씩 껐다 켜기 → **다른 칸의 폭이 그대로인가**
2. 월 마지막 수업 주차를 고르기 → **김밥신청이 자동으로 켜지는가**
3. 김밥신청을 끈 뒤 주차 이동 → **꺼진 채로 유지되는가** (사람 판단 존중)
4. `Ctrl+P` → **화면 미리보기와 인쇄 미리보기가 같은가** (특히 음영)
5. 몇 장을 끄고 인쇄 → **끈 장이 빠지고 마지막에 빈 종이가 없는가**
6. 전부 끄고 인쇄 → **막히는가**
7. 조원 12명 / 5명 / 25명인 조 → **각각 아래 여백이 자연스러운가**
8. 좁은 화면에서 → **출력 체크가 잘리지 않는가**

---
